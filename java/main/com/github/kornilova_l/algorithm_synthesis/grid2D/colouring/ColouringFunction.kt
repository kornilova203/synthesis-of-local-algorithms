package com.github.kornilova_l.algorithm_synthesis.grid2D.colouring

import com.github.kornilova_l.algorithm_synthesis.grid2D.tiles.TileMap

class ColouringFunction internal constructor(private val tileColours: TileMap<Int>) {

//    fun getColour(grid: Grid2D, i: Int, j: Int): Int {
//        val tile = Tile(grid, tileColours.n, tileColours.m, i, j)
//
//    }
}
